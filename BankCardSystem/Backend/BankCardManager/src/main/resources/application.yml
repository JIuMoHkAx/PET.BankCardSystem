spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db
    username: bankdbuser
    password: bankdbpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  security:
    server-salt: "Deucalion"